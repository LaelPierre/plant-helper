<div class="card-plant container">
  <% @plants.each do |plant| %>
    <%= cl_image_tag plant.photos.first.key, width: 125, height: 125, crop: :fill, class: "image-border" %>
    <div class="card-plant-infos">
      <div>
        <h2><%= plant.species %></h2>
        <h3><%= plant.name %></h3>
      </div>
    </div>
  <% end %>
</div>
